/**
 * @file アプリケーションメッセージ定数
 * @see req-005-001, req-005-003, req-005-004, req-005-005, req-005-006, req-005-009
 */
import { QUERY_MAX_LENGTH } from "./app"

/**
 * @remarks
 * システムが「状態・結果」に応じてユーザーへ伝える通知テキスト
 * （エラー・バリデーション失敗・検索結果・ページ状態）。
 * ボタン名・プレースホルダー等のUI文言は {@link ../labels} を参照。
 *
 * ID 体系:
 *   MSG-C001〜 : 共通メッセージ（複数画面で使用）
 *   MSG-001〜099: 検索機能
 *   MSG-101〜199: リポジトリ詳細
 *   MSG-201〜299: GitHub API エラー
 *   MSG-301〜399: 共通ページ（404 等）
 *
 * テンプレートメッセージ（動的パラメータあり）は {@link MSG_TPL} を参照
 */

/**
 * 静的メッセージ定数
 */
export const MSG = {
  // ── 共通 (MSG-C001〜) ──────────────────────────────────────────

  /** MSG-C001: 予期しないエラーが発生したときの汎用メッセージ */
  "MSG-C001": "予期しないエラーが発生しました",
  /** MSG-C002: リトライを促す汎用メッセージ */
  "MSG-C002": "しばらくしてからお試しください",

  // ── 検索機能 (MSG-001〜099) ────────────────────────────────────

  /** MSG-002: 検索クエリ文字数超過バリデーションエラー */
  "MSG-002": `検索キーワードは${QUERY_MAX_LENGTH}文字以内で入力してください`,
  /** MSG-008: 検索結果なし時のサジェスション */
  "MSG-008": "別のキーワードで検索してみてください",

  // ── リポジトリ詳細 (MSG-101〜199) ──────────────────────────────

  /** MSG-101: リポジトリ取得失敗時のエラーメッセージ */
  "MSG-101": "リポジトリの取得に失敗しました",
  /** MSG-102: リポジトリが見つからない場合のメッセージ */
  "MSG-102": "リポジトリが見つかりませんでした",
  /** MSG-103: リポジトリが見つからない理由の補足説明 */
  "MSG-103": "リポジトリが削除されているか、非公開になっている可能性があります",

  // ── GitHub API エラー (MSG-201〜299) ───────────────────────────

  /** MSG-201: GitHub API レート制限 (HTTP 403) */
  "MSG-201": "API レート制限に達しました。しばらく待ってから再試行してください。",
  /** MSG-202: GitHub API 無効なクエリ (HTTP 422) */
  "MSG-202": "検索クエリが無効です。別のキーワードをお試しください。",

  // ── 共通ページ (MSG-301〜399) ───────────────────────────────────

  /** MSG-301: グローバル 404 ページのメッセージ */
  "MSG-301": "ページが見つかりませんでした",
} as const

/**
 * テンプレートメッセージ（動的パラメータあり）
 * @remarks 静的メッセージは {@link MSG} を参照
 */
export const MSG_TPL = {
  /**
   * MSG-007: 検索結果なし（キーワードを含む）
   * @param q - 検索キーワード
   * @returns 検索結果なしメッセージ
   */
  "MSG-007": (q: string) =>
    `「${q}」に一致するリポジトリが見つかりませんでした`,
  /**
   * MSG-203: GitHub API 汎用エラー（ステータスコードあり）
   * @param status - HTTP ステータスコード
   * @returns API エラーメッセージ
   */
  "MSG-203": (status: number) => `GitHub APIエラーが発生しました（${status}）`,
  /**
   * MSG-204: リポジトリ取得エラー（ステータスコードあり）
   * @param status - HTTP ステータスコード
   * @returns リポジトリ取得エラーメッセージ
   */
  "MSG-204": (status: number) =>
    `リポジトリ情報の取得に失敗しました（${status}）`,
} as const

export type MsgKey = keyof typeof MSG
export type MsgTplKey = keyof typeof MSG_TPL
